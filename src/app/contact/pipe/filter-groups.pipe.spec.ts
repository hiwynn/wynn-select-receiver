import { FilterGroupsPipe } from './filter-groups.pipe';

describe('FilterGroupsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGroupsPipe();
    expect(pipe).toBeTruthy();
  });
});
